/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Opt out of Turbopack for stable Pages Router compatibility
  experimental: {},
};

module.exports = nextConfig;
